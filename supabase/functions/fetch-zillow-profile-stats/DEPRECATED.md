# DEPRECATED

This function has been deprecated in favor of using only:
- `agenscrape~zillow-agents-finder` (via fetch-getdataforme-agent-stats)
- `getdataforme~zillow-real-state-agents-scraper` (via fetch-zillow-agents)

All calls should now use `fetch-getdataforme-agent-stats` or `update-agent-zillow-stats` instead.
