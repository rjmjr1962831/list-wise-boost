// Deprecated Sonner wrapper kept only for backward compatibility.
// All new code should use the shadcn toast system via `useToast`.
// Re-exporting Toaster from the correct location to prevent import errors.
export { Toaster } from "./toaster";
